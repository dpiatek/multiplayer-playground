import ReactDOM from "react-dom/client";
import PresenceMemberApp from "./presence-member/App";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <PresenceMemberApp />
);
